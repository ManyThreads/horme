{
  "name": "horme",
  "version": "0.0.1",
  "description": "horme configuration and re-configuration",
  "bin": "bin/app.js",
  "license": "MIT",
  "scripts": {
    "start": "npm run tsc && node bin/app.js",
    "tsc": "./node_modules/.bin/tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Oliver Giersch",
  "devDependencies": {
    "@types/node": "^14.0.27",
    "tslint": "^6.1.3",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "async-mqtt": "^2.6.1"
  }
}